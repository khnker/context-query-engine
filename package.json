{
  "name": "context-query-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Context query language, optimizer y motor de retrieval para agentes LLM",
  "type": "module",
  "scripts": {
    "check-tools": "scripts/check-tools",
    "test": "node --test test/cqp.test.js test/interpreter.test.js test/optimizer.test.js test/statistics.test.js test/local-model.test.js && bash test/smoke.sh && bash engine/test-e2e.sh",
    "eval": "bash evals/run-eval.sh && bash evals/analyze-eval.sh --in evals/reports/latest.ndjson"
  }
}
